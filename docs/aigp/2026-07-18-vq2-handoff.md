# AIGP VQ2 build 3385 handoff

> Current as of 2026-07-20 on John's Windows host. Live evidence remains from
> 2026-07-18. This document supersedes the VQ1 pose-and-gate-map assumptions in
> older AIGP handoffs for build 3385.
>
> Offline development after Wave 3E and the bounded promotion-hygiene
> maintenance resumes from
> `2026-07-20-vq2-development-continuation-handoff.md`; this file remains the
> authority for live state and safety.

## Current status

The VQ2 transport, camera receiver, red-gate detector, IMU attitude estimator,
and safety-gated training runner are implemented and covered by tests. Passive
preflight, bounded control-sign calibration, a simulator-credited first-gate
pass, and bounded post-pass gate-1 reacquisition all succeed live.

This is **not race-ready**. Gate 0 passed cleanly and gate 1 was observed for
three consecutive frames, but no control command has steered toward or tried
to pass gate 1, and no lap has been completed. The next live simulator step is
a separately reviewed, bounded gate-1 recentering controller, not a full race.

## Offline Wave 1 update -- no new live evidence

Offline record `1cf17ea5f4e0a330bee89b0128d30b13657899a2`, containing
candidate `361d0060f16dbaec753de00ba491f1a085707eb1`, adds exact
production-frame timing publication plus pure latest-value/scheduler utilities,
an opt-in visible/censored inner-aperture fitter, and a feature-space relative
estimator. A cross-workstream test binds the latest-value timing/cursor,
detector, aperture fitter, and estimator from an exact synthetic, read-only,
timed `VQ2VisionSnapshot`. It proves bbox-only geometry is withheld, but does
not exercise receiver publication or scheduler execution.

The powered runner still uses its legacy bbox tracking/control path. The new
geometry, estimator, and pure scheduler do not send commands. The only narrow
runner change is distinct-frame identity based on `(generation, frame_id)`,
covered offline but not re-flown.

Candidate evidence is 187 combined affected tests, exactly 418 VQ2 policy tests
in canonical and isolated runs, and 1,510 fast/unit tests with 20 expected skips
and 42 deselections. No FlightSim process launch or connection, preflight,
external network access, reset, arm/disarm, target, or powered command occurred
during this wave. Offline tests may use local Python workers and loopback test
sockets. Post-merge `test-vq2` on main also passed all 418 tests; tracked Git
status was empty. All prior live results and safety limits below remain
unchanged.

## Offline Wave 2 update -- no new live evidence

Integrated candidate `8176cbac20ff16bfa4b8c24764596d9366fe98cf` adds a
pure predictive controller, offline system-identification tooling, mapless
guidance/state logic, and an exact offline guidance/controller adapter. The
adapter owns the guidance call in this composition, threads accepted memory,
latches the Gate 0 pitch basis, exact-binds guidance/state/tick provenance, and
maps only Gate 0 approach and Gate 1 align/recenter. Every other mapping,
including commit and gates above one, produces source-less exact zero.

A generated cross-layer test now carries an immutable, already-decoded timed
image through detection, aperture fitting, relative estimation, guidance, and
controller proposal validation over multiple distinct frames. It proves that
bbox-only geometry remains withheld. It does not exercise JPEG receiver or
reassembly, a recorded capture, measured plant response, official-simulator
behavior, passage, or powered flight.

Accepted evidence is 36 adapter tests, 378 combined affected tests, exactly 743
VQ2 policy tests in canonical, isolated-manifest, committed-candidate, and
post-merge runs, and 1,835 fast/unit tests each with 20 expected skips and 42
deselections. The promotion-boundary full non-live suite passed 1,876 tests
with 21 skips in 500.10 seconds; skipped optional coverage is not positive
evidence. The reviewed trusted manifest contains 123 entries. Main
fast-forwarded from `e9a416714b01c2845786a0a22b168a9037f379ec` and the
post-merge VQ2 run passed all 743 tests in 29.64 seconds with empty tracked Git
status.

At the Wave 2 boundary, attitude and the Gate 0 pitch basis still lacked
timestamp/source correlation, caller-threaded pure memory was a trust boundary,
no approved replay corpus/final processor was present, and measured delay/plant
plus tracker-isolation replay evidence was absent. No FlightSim process launch
or connection, preflight, external network access, reset, arm/disarm, target,
transport, or powered command occurred. All live results and safety limits
below remain unchanged.

## Offline Wave 3 IMU update -- no new live evidence

Integrated record `ecaa794aeaed87a169b7b87b284d1440f1768a28`, containing
behavioral implementation `f53718da892c4ab5aecc567a61249b21a8cb6ffa`
and main reconciliation merge
`e3f386d460d012c7b9710ae440c2ac405447f1f3`, adds exact local
HIGHRES_IMU/attitude provenance, a bounded pure rotation-only camera-ray
correction, and an offline outer adapter around the unchanged Wave 2
guidance/controller composition.

The adapter exact-binds session/reset, host clock, IMU stream/generation,
sample sequence, source ordering, per-sample host receipt, camera observation,
prediction target, calibration/model identity, and controller tick. It derives
and latches the Gate 0 pitch basis from the accepted phase-entry attitude,
separately propagates controller attitude to proposal time, and emits
source-less exact zero on any provenance, chronology, freshness, uncertainty,
or pitch-source failure. The ordinary raw-camera estimator result remains
bit-for-bit unchanged. Its rotation-corrected bearing is standalone evidence;
it is not injected into the capture-time filter, guidance, or frozen `/1`
proposal.

Accepted offline evidence is 143 affected tests, exactly 872 VQ2 policy tests,
and `test-fast` plus `test-unit` results of 1,967 passed, 20 skipped, and 42
deselected each. The promotion-boundary full non-live suite passed 2,008 tests
with 21 skips. The strict trusted manifest contains 126 files, with semantic
identity
`f074019f30858b9fcc5fb06a90a8df7cf57770e84791893ddbaa082861eca5eb`
and file SHA-256
`ba07b6ea73b5fc88f99e6c8824ea4d7039c956391de2c5730e6716af76cad9b1`;
the exact VQ2 policy file SHA-256 is
`4352163c57b06f8bb12a7b7750c8a279d76b0c45d933dacf3d5149238ee970ef`.
Independent review cleared the integrated candidate. Main fast-forwarded to
`ecaa794aeaed87a169b7b87b284d1440f1768a28`; post-merge `test-vq2`
passed all 872 tests and tracked Git status was empty.

This remains deterministic generated/offline evidence only. Frozen
`CommandProposalV1` cannot carry attitude, pitch-basis, calibration, or
derotation identity to the supervisor. Runtime promotion still requires a
reviewed `/2` envelope or supervisor-owned registry, production per-sample host
arrival capture, and calibrated camera/IMU timing and extrinsics. Applying the
corrected ray still requires a reviewed stable-frame or explicitly time-aligned
filter. No FlightSim process was launched or contacted, and no preflight,
external network access, reset, arm/disarm, target, transport, shadow,
simulator, or powered action occurred. All established live results and safety
limits remain unchanged.

## Offline Wave 3B generated runtime update -- no new live evidence

Integrated promotion record
`28b7d782404d6b825cebae3b65a8443d756be234`, containing behavioral
implementation `8eab146e3a9a7a1a1b28070d3e0234adff900595` and main
reconciliation merge `7904fbadbc4b220b81afb846a69b15a7b30ef4bb`, adds a
deterministic generated/offline owner for the exact latest-frame cursor, 50 Hz
scheduler, local IMU provenance/history, raw-camera relative estimator, Wave 3
adapter memory, proposal sequence, and cumulative latency trace.

The composition consumes exact already-decoded immutable snapshots and timed
IMU occurrences, runs perception only once per distinct publication, chooses
bounded capture/target attitudes available by the decision time, and stops at
a quarantined `CommandProposalV1`. Repeated frames run no perception and remain
source-less exact zero. Expired and planned-overrun ticks skip before pipeline
state advances and never catch up. The corrected ray remains standalone; the
ordinary relative state, guidance, and proposal retain the raw-camera basis.

The mutable nested `CameraFrame`, exported result, and cumulative trace are
revalidated structurally. Exact camera metadata/storage and independent legacy
freshness semantics are enforced. Current tick/lease/proposal, camera stages,
perception success or failure, dropped-frame evidence, and selected IMU facts
must agree before commit. `GYRO_SAMPLE` remains an occurrence-only fact with no
frame, tick, command, send, actuator, or response-causality claim.

Accepted evidence is 38 focused runtime tests, 181 coupled tests, and an
independent 199-test compatibility/adversarial matrix with explicit clearance.
Canonical and isolated-manifest VQ2 policy runs passed exactly 910 tests;
`test-fast` and `test-unit` each passed 2,005 with 20 skips and 42 deselections;
and `test-full-non-live` passed 2,046 with 21 skips. The strict 127-file
trusted manifest has semantic identity
`cdd0db402b6f1c8bb0c90c1b8d445ca64741d3bfc3aa03a78c3fe4d73c8dcce2`,
file SHA-256
`e270a194031d463accfb50b28bd3296eb672004d1c41241fab3cb368bab1640a`,
and policy SHA-256
`64cfefc083a52fc925ad98c2e3a99e8f6eefcaebb0f4243d214c1e87729a864c`.
Main fast-forwarded to the promotion record and post-merge `test-vq2` passed all
910 tests with empty tracked status before documentation closeout.

This is still generated mechanism evidence, not receiver/reassembly, recorded
replay, measured timing, shadow/runtime, supervisor, send, actuator, or powered
evidence. No FlightSim process was launched or contacted, and no preflight,
external network access, reset, arm/disarm, target, transport, shadow, or
powered action occurred. All live results and safety limits remain unchanged.

## Offline Wave 3C correlated coast update -- no new live evidence

Behavioral implementation
`84674fd8c7379b327e25725010ca58a57f4fd910` and promotion/trust closeout
`168220ba7060d07743335d0e9c56bcd2d05d669d` add a default-off,
proof-bound one-tick correlated coast to the generated Wave 3 runtime. A
healthy accepted update may arm one lease only when its source scheduler tick
starts exactly on due. A valid late source remains accepted but opens no lease
after the scheduler rebases.

Only the immediate repeated-frame successor may advance constant-velocity
prediction state to first-dropout `COASTING`. It must use a strictly newer
causal same-source attitude and strictly increase marginal uncertainty. Public
guidance, controller, and Wave 2 paths still reject dropout; only the private
Wave 3-owned capability path may permit this narrow coast. A skip, new-frame
selection or failure, lifecycle mismatch, invalid evidence, coast success, or
reuse consumes the lease, and the second repeat remains source-less exact zero.
Every consuming result retains and reconstructs its exact prior source
transition and exact camera, perception, IMU, scheduler, estimator, controller,
terminal, and cumulative-trace facts before commit.

Accepted offline evidence is 74 focused runtime tests, 95 Wave 3 adapter tests,
32 relative-estimator tests, a 477-test six-module affected matrix, and an
independent 201-test deep contract matrix with explicit clearance. Canonical
and isolated-manifest VQ2 policy runs pass exactly 1,019 tests, including the
post-merge main run. `test-fast` and `test-unit` each pass 2,114 tests with 20
skips and 42 deselections; promotion-boundary `test-full-non-live` passes 2,155
tests with 21 skips.

The strict trusted manifest remains 127 files, with semantic identity
`f9118fad5fdbdd8e5e355cf0e153492525b853b9b7c32239ab4d2d81f6d63b2b`, file
SHA-256
`29b306e41a6954552ef7693f0e0c3d853cc4b60aeedfb59f6a2c9592ece9d8c6`, and
policy SHA-256
`29eb2dcd627a8f5dbbea4bf88c249a87ca741ca5c9d743c0c646404f40e8748e`.
The trust review replaced exactly six changed test hashes plus the policy hash,
with no file addition/removal or trust-root expansion.

This remains generated mechanism evidence, not receiver/reassembly, recorded
replay, calibrated timing/extrinsics, supervisor, stable-frame corrected-ray,
measured response, shadow/runtime, or powered evidence. No FlightSim process
was launched or contacted, and no preflight, external network access, reset,
arm/disarm, target, transport, shadow, or powered action occurred. All live
results and safety limits remain unchanged.

## Offline Wave 3D stable-reference update -- no new live evidence

Contract freeze `ede0edca3025ad03db3032e371a37c86dc8fdc00`, behavior
`c21a742004d1d3bc485a866babb9759b6aee62fb`, and promotion/integration
`46df0adee76070e10509fa5e807b986a9469c68e` add a standalone,
bidirectional stable-orientation transform for a distinctly named local
differential feature state. It has no estimator, adapter, runtime, controller,
supervisor, transport, or powered call site.

The prototype freezes exact capture/target time binding, a seed-bound reference
fingerprint, camera/authority/IMU/model lifecycle, determinant-one projective
math, bearing/rate/expansion chain rules, a dense analytic `6x6` Jacobian, and
separated covariance terms. It also freezes the critical semantic limit: frozen
`/1` `log_scale` is finite fitted-quadrilateral area and cannot be transformed
exactly from the present six-state summaries. Wave 3D instead uses
`vq2-local-differential-area-v1`; it never accepts or emits a `/1` feature state.

The covariance scope prevents accidental direct reuse of total-labelled output
but remains a caller assertion. The acceleration bounds and declared joint
nuisance envelope are model assumptions whose dominance is not derived by this
module. Production application still requires an honest full-homography local-
scale measurement/covariance producer or a shape-augmented finite-quad state,
sequential nuisance treatment, calibrated timing/extrinsics, replay, estimator
integration, and separate runtime/authority review.

Accepted offline evidence is 82 direct tests, a 186-test compatibility matrix,
three independent final clearances, exactly 1,101 canonical and isolated VQ2
tests, 2,196 fast/unit passes with 20 skips and 42 deselections each, and 2,237
full non-live passes with 21 skips. Post-merge main `test-vq2` passed all 1,101
tests. The 128-file trusted manifest has semantic identity
`2f70415dd7cdfa0675c6dc778406cdccfdca09757e79b1a8f1a3e0d4752e9268`
and file SHA-256
`2c965f2f5a6486f506d51c8e290b09d6a22166f6f277fbff1234690e510d63d9`;
policy file SHA-256 is
`a98b2d4d618b6999927d1c997ca0a65c63aebef742c53bef31a6c05dcd53b020`.

No FlightSim process was launched or contacted, and no preflight, external
network, replay, reset, arm/disarm, target, transport, shadow/runtime, or
powered action occurred. All live results and safety limits below remain
unchanged.

## Offline Wave 3E local-differential measurement update -- no new live evidence

Contract freeze `c7dcb612318eb9d26868fa1364c1a027d2b8edcd`, review-driven
correction `aab44d48a032444faeaf5cd1020e90dc9dbd24ed`, behavioral
implementation `ceed9c854b0066d4f00d4add796fb968d449593a`, and
promotion/integration `16dd5c84995cafb5158e277d730e670557ba69f2` add a
standalone immutable reducer from one externally produced, rectified,
center-gauge-fixed full homography and dense conditional `8x8` covariance to
the instantaneous camera-measurement-time value
`[bearing_x_norm, bearing_y_norm, local_log_scale]`.

The reducer freezes exact frame, candidate, authority, timing,
producer/configuration, calibration, fit-model, and covariance-model lineage;
the `vq2-local-differential-area-v1` semantic; an analytic `3x8` Jacobian;
dense conditional covariance congruence; and bit-exact same-process evidence
rederivation. It does not obtain a homography from the detector, accept current
`/1` observations or fitted corners, rectify pixels, estimate rates, create an
estimator state or association, call Wave 3D, or feed a production path. The
current five `/1` summaries cannot be lifted uniquely into an honest full-
homography or local-scale covariance.

Accepted offline evidence is `224` direct tests and a `450`-test focused
compatibility matrix. Canonical, cache-clean isolated, and post-merge main VQ2
runs each passed exactly `1,325` tests. The fast and unit tiers each passed
`2,420` with `20` skips and `42` deselections. Promotion-boundary
`test-full-non-live` passed `2,461` tests with `21` skips. The post-merge main
run completed in `33.72s` with empty tracked status before documentation
closeout. The `129`-file trusted manifest matched all `129` files; its
semantic identity is
`46e77cbbe8a131517444b141293b1fe8c2bab546a6f5630f711ffe0d621d5ea2`
and file identity is
`e88363ef096bba83fe4660a4903abb6ae063f41682246b38ba9c69481008fffc`.

This remains T0 proof-only evidence. It adds no full-homography producer,
calibration artifact, recorded replay result, association, rates, filter,
estimator input, runtime selection, supervisor carrier, or authority claim. No
FlightSim process was launched or contacted, and no preflight, external
network, replay, reset, arm/disarm, target, transport, shadow/runtime, or
powered action occurred. All live results and safety limits below remain
unchanged.

## Offline telemetry-artifact isolation maintenance -- no new live evidence

Behavioral commit `5a9fa4ae231fec4ede4476157b72b090249a59a9` and
promotion/integration commit `ef92041bb3f05b1d8f3ef69182db8d51184c9cce`
isolate the slow VQ1 dry-run test's telemetry artifact under pytest `tmp_path`.
The only behavioral file changed was the test; the task record and existing
trusted-manifest mapping also changed. `scripts/aigp_vq1_run.py` and its
intentional default recording behavior are byte-unchanged. This is promotion
hygiene and changes no VQ2 production or live behavior.

The VQ2 policy remains byte-identical with file SHA-256
`7daa46ec4dfd025c18f12076add06d70b6463f07d6320b20487a63bd78d0851e`
and exactly `1,325` expected passes. The trusted manifest remains 129 files;
only the VQ1 runner-test digest changed to
`977f2431aaa07b762eab7888451f0b6aa82dc5aa6f387d940d3862d3ecb9cf07`.
Its current file and canonical JSON identities are respectively
`3855243e7b3675ebff14731bbd073b7850bb87fb9d9d35267b7ca0fa2982d08f`
and `ac2700e5cfed1c9aece92446d7aef665ddfff923d790e62628c35cbbbf4978a2`;
independent review matched all `129/129` files with no path addition/removal.

The promotion full suite passed `2,461` tests with `21` skips and created no
repository-local telemetry capture. A separate pristine candidate passed all
`1,325` hash-pinned VQ2 tests, and the post-merge main run passed all `1,325`
canonical VQ2 tests. No FlightSim process was launched or contacted, and no
preflight, external network, replay, reset, arm/disarm, target, transport,
shadow/runtime, or powered action occurred. All live results and safety limits
below remain unchanged.

## Host and simulator

- Repository: `C:\Users\John\killallhumans`
- Simulator: `C:\Users\John\AIGP\AIGP_3385\FlightSim.exe`
- Official devkit: `C:\Users\John\AIGP\devkit_3385\PyAIPilotExample-v2`
- Python environment: `C:\Users\John\killallhumans\.venv` (Python 3.12)
- Simulator mode: **Training**
- MAVLink: UDP `127.0.0.1:14550`
- Camera: UDP port `5600`, 640x360 BGR after JPEG decode

FlightSim may be windowed and unfocused. It does not need to be full screen.
It must stay running, unminimized, and unpaused so the camera stream continues.
The process was responsive at handoff. Process IDs are transient and should not
be treated as configuration.

## Safe restart sequence

From PowerShell in the repository:

```powershell
.\.venv\Scripts\python.exe -m scripts.aigp_vq2_run --stage preflight --record
```

`preflight` is passive: it does not arm or send flight targets. It requires a
fresh heartbeat, IMU, race status, actuator stream, camera stream at at least
20 decoded frames/s, a calibrated attitude estimate, and a stable gate target.

2026-07-22 process update: `flight-cycle` folds those readiness conditions,
including the 20 decoded frames/s and exact 640x360 checks, into the post-reset
countdown before arm. A separate passive preflight remains available for
diagnostics but is not an additional prerequisite on that fast path.

For the legacy direct-runner workflow, only after preflight passes, the
already-calibrated bounded sign check is:

```powershell
.\.venv\Scripts\python.exe -m scripts.aigp_vq2_run --stage sign-id --record
```

Other powered stages exist for development:

```powershell
.\.venv\Scripts\python.exe -m scripts.aigp_vq2_run --stage hover --record
.\.venv\Scripts\python.exe -m scripts.aigp_vq2_run --stage gate0 --record
.\.venv\Scripts\python.exe -m scripts.aigp_vq2_run --stage gate0-observe --record
```

Treat `hover`, `gate0`, and `gate0-observe` as explicit live-flight tests, not
routine startup checks. `gate0` is bounded to five seconds and stops as soon
as race status advances on a strictly newer packet. `gate0-observe` preserves
that same bound, sends only exact zero commands after credit, and waits at most
0.20 seconds for three distinct gate-1 frames before cleanup. Neither stage
attempts gate 1.

`--record` chooses a timestamped compressed JSONL path under `captures/`.
Captures are intentionally ignored by Git.

## Build 3385 interface findings

Unlike the older VQ1 build, VQ2 build 3385 does not provide usable
`ATTITUDE`, `LOCAL_POSITION_NED`, `ODOMETRY`, or gate-map geometry. The working
inputs are:

- `HIGHRES_IMU`
- UDP JPEG vision
- heartbeat
- race status
- actuator output status
- collision events

The runner therefore creates `AIGPMavlinkAdapter` with
`telemetry_mode="imu"` and `fetch_track_on_connect=False`. It commands only
body rates plus thrust with `SET_ATTITUDE_TARGET`'s attitude-ignore mask. The
adapter intentionally rejects quaternion-attitude commands in IMU mode.

Measured VQ2 desired-rate-to-gyro signs are `(-1, -1, -1)`. The legacy pose
mode mapping remains `(-1, +1, -1)`. Yaw control in the VQ2 runner is exactly
zero because yaw sign/authority has not been safely calibrated in flight.

The simulator repeats each UDP JPEG chunk roughly 50-60 times, producing about
55,000 datagrams/s. `competition.vq2_vision.VQ2VisionThread` suppresses chunks
by `(frame_id, chunk_id)` before decode and isolates that load from the 50 Hz
control loop. Do not replace it with synchronous polling in the runner.

## Implementation map

- `competition/aigp_mavlink.py`: IMU-only transport mode, explicit stream
  freshness, reset semantics, arm/disarm confirmation tokens, and safe
  attitude-rate transmission.
- `competition/vq2_vision.py`: dedicated duplicate-suppressing camera thread
  with reset generations, fresh snapshots, and exact publication timing.
- `competition/vq2_runtime.py`: pure latest-value cursor, fixed-rate scheduler,
  latency trace, and percentile-summary utilities; not wired to transport.
- `competition/vq2_controller.py`: pure bounded Gate 0 approach and Gate 1
  recenter proposal generation; no supervisor or transport authority.
- `competition/vq2_wave2_adapter.py`: offline-only guidance/controller
  composition with exact mapping, provenance, and source-less-zero behavior.
- `competition/vq2_wave3_imu_adapter.py`: offline-only exact IMU/attitude,
  Gate 0 pitch, estimator, guidance, controller provenance gate, and private
  first-dropout coast capability.
- `competition/vq2_wave3_offline_runtime.py`: generated/offline latest-frame,
  IMU, raw estimator, Wave 3 adapter, fixed-rate scheduler, and exact trace
  composition, including the default-off immediate-successor coast lease and
  exact consumed-source reconstruction; terminates at a quarantined proposal.
- `competition/vision_udp.py`: cache/frame-ID reset behavior shared with the
  older listener.
- `estimation/imu_attitude.py`: stationary bootstrap followed by gyro-only
  quaternion propagation for powered flight.
- `estimation/vq2_relative_estimator.py`: offline raw-camera feature-space
  filter, innovation gating, coasting/loss, bounded prediction, and a local IMU
  correlation envelope; the corrected bearing is not applied to filter state.
- `estimation/vq2_imu_provenance.py`: exact timestamped HIGHRES_IMU lineage,
  host-arrival correlation, and bounded attitude propagation evidence.
- `estimation/vq2_imu_derotation.py`: pure bounded rotation-only camera-ray
  correction with explicit calibration/model identity.
- `estimation/vq2_stable_reference.py`: standalone bidirectional stable-
  orientation transform for the distinct local-differential feature state; no
  `/1`, estimator, or runtime call site.
- `estimation/vq2_local_differential_measurement.py`: standalone reducer from
  externally supplied rectified full-homography evidence to the local three-
  component conditional measurement; no detector producer, sequence, filter,
  or runtime call site.
- `estimation/vq2_system_id.py`: bounded offline experiment, fitting, and
  profile-selection tooling; profiles are not runtime-authoritative.
- `gate_detection/src/vq2_detector.py`: build-3385 red/pink HSV gate preset.
- `gate_detection/src/vq2_geometry.py`: opt-in visible/censored inner-aperture
  fit with image-space uncertainty.
- `planning/vq2_guidance.py`: mapless gate-scoped guidance, lifecycle, and
  active/shadow track ownership; never sends commands.
- `scripts/aigp_vq2_run.py`: bounded preflight, sign-ID, hover, first-gate, and
  post-pass gate-1 observation stages plus capture logging.
- Corresponding tests live beside each component and in
  `tests/test_aigp_vq2_runner.py`.

The detector's metric distance and corner fields are placeholders. The runner
uses only filtered pixel geometry and never treats those fields as calibrated
3D measurements. The new inner-aperture fitter is opt-in, remains image-space
only, and has not been used by the live runner. Neither its fitted corners nor
the frozen `/1` covariance satisfies the Wave 3E input contract.

## Safety contract

Do not weaken these invariants while iterating:

1. A powered stage must prove **both** race-clock and IMU-clock rollback after
   `SIM_RESET`, observe multiple advancing samples, and witness countdown.
2. Stop the vision socket during reset and restart it only after the new epoch
   is proved; camera frame IDs/timestamps do not roll back in this build.
3. Wait until GO plus 150 ms before arming or sending powered commands.
4. Confirm arm/disarm only on a heartbeat newer than the arm/disarm attempt.
5. Pace commands no faster than 50 Hz, dropping missed ticks rather than
   replaying them, below the simulator's 100 Hz command limit.
6. Abort on stale/non-advancing IMU or race data, stale camera/target, estimator
   failure, unsafe collision, non-finite command, excessive angle/rate, or a
   command outside the calibrated envelope.
7. Keep commanded yaw rate at zero and clamp roll/pitch command rates to
   0.25 rad/s.
8. Cleanup must always send reset, then attempt to prove the clean epoch and
   disarmed state. A failed cleanup means the stage result is failed.
9. Once a credible close crossing loses the target, latch zero-rate/zero-thrust
   and wait at most 0.40 s for a strictly newer race packet. Vision alone never
   declares a pass.
10. `hover`/`gate0` may tolerate at most 12 tiny launch-pad contacts totaling
    0.05 impulse during the first 0.35 s; later or larger contacts abort.
11. Post-pass observation requires a proved same-epoch gate `0 -> 1` race
    transition, resets only the stale target tracker, sends only exact zero
    commands, preserves every non-target watchdog, and ends within the earliest
    of pass + 0.20 s, crossing confirmation + 0.40 s, or flight start + 5.0 s.
    Three distinct newer frames are required; observation never authorizes a
    gate-1 attempt.

Training can initially report heartbeat `base_mode=193` (armed bit set) while
actuator demand is zero. Powered stages normalize this to disarmed after their
proved reset instead of trusting the boot state.

## Live results already established

- Passive preflight: about 31 decoded frames/s with fresh required streams.
- Spawn attitude: roll near 0, pitch about `-0.3107 rad` (`-17.80 deg`).
- Initial gate: approximately bbox `(282, 134, 80, 80/81)`, center `(322, 174)`.
- Clean sign-ID: positive roll request measured `+0.080825 rad/s`; positive
  pitch request measured `+0.158619 rad/s`; maximum attitude excursion was
  `0.018711 rad` (about 1.07 deg); cleanup confirmed.
- Stronger 0.32-thrust launch stabilized roll/pitch and produced a repeatable,
  centered approach.
- Gate 0 passed collision-free. Confirmation armed at bbox
  `(75, 0, 518, 360)`, cut thrust when the full-frame target became stale, and
  accepted gate index 1 only when race time advanced from 5996 to 6246 ms.
- The bounded observation repeat also passed gate 0 collision-free, with the
  decisive race packet advancing from 6005 to 6256 ms. All three post-pass
  setpoints were exact zero commands, spaced about 31 ms apart.
- Gate 1 was already detectable before race credit, but the stale full-frame
  gate-0 tracker correctly rejected it. A tracker-only reset then acquired
  three consecutive frames in 0.093 s: bbox `(477, 4, 106, 97)`, then
  `(477, 0, 112, 99)`, then `(479, 0, 115, 97)`, with center moving from
  `(530, 52)` to `(536, 48)`.
- At acquisition the attitude was stable near roll `0.00320 rad` (`0.18 deg`)
  and pitch `-0.05094 rad` (`-2.92 deg`), with peak body rate below
  `0.002 rad/s`. The next gate was high-right and partly top-clipped.
- Cleanup logged gate index 1 before reset, then confirmed disarm and the new
  reset epoch. No collision or abort event was recorded.
- No steering toward gate 1 and no full-race attempt has occurred.

The most useful final trace is now:

```text
captures/vq2_gate0-observe_20260718T022458.jsonl.gz
```

Its three associated PNGs capture gate-0 visual loss, authoritative gate-1
race credit, and the exact third acquired gate-1 frame. The earlier
`vq2_gate0_20260718T015006.jsonl.gz` records the first credited gate-0 pass.
The preceding trace,
`vq2_gate0_20260718T011052.jsonl.gz`, followed essentially the same path but
reset exactly as the decisive 4 Hz race packet arrived. That run was
observationally ambiguous, not a proven miss. The current runner explicitly
waits for one newer authoritative packet after a credible close crossing.

## Next engineering step

The current trust-integration baseline is
`ef92041bb3f05b1d8f3ef69182db8d51184c9cce`. It adds only the completed
test-artifact isolation and changes no production or live behavior.

The generated, already-decoded image/IMU path, fixed-rate scheduler, Wave 3
adapter, exact trace composition, and default-off Wave 3C one-tick correlated
coast are complete offline at
`168220ba7060d07743335d0e9c56bcd2d05d669d`. The standalone Wave 3D stable-
reference transform is complete at
`46df0adee76070e10509fa5e807b986a9469c68e`, and the standalone Wave 3E full-
homography-to-local-measurement reducer is integrated at
`16dd5c84995cafb5158e277d730e670557ba69f2`. Wave 3D and Wave 3E remain
standalone mathematical evidence with no production call site.

Wave 3E closes only the downstream reducer algebra for the full-homography
branch. It does not provide the homography or its covariance and does not make
current `/1` observations or fitted corners admissible. An actual producer
remains blocked on calibrated image-to-ray rectification, an independently
reviewed dense full-homography fit covariance, explicit shared
calibration/model nuisance, and validation against approved recorded replay.
The alternative shape-augmented finite-quad path would still require a new
provenance envelope, rates, and dense covariance. Do not synthesize either
input from `/1` summaries, border-normalized pixels, heuristic floors, or
independence assumptions.

The separate replay path remains to provision approved recorded inputs and the
final processor, then exercise operational T0-T4 without relabeling generated
images as recorded replay. Tracker-isolation scoring remains blocked until
those inputs and processor exist.

Do not combine the completed Wave 3C mechanism with stable-reference filtering.
Wave 3D supplies only the reviewed stable-reference lifecycle and bidirectional
transform; Wave 3E supplies only one instantaneous conditional measurement
reducer. Neither supplies cross-frame association, rates, a sequential filter,
retained shared-nuisance cross-covariance, production calibration,
estimator/runtime wiring, or a supervisor-verifiable proof carrier. Until those
prerequisites exist, stop at the standalone evidence rather than adding zero
rates, finite differences, a synthetic producer, or unreviewed filter wiring.

Do not claim full detection-through-gyro response latency from that generated
composition. Production per-sample IMU arrival, supervisor approval and actual
send, actuator response, causal gyro correlation, and calibrated clock/delay
models remain absent. Do not select scheduler output for transport without
separate powered authorization. Applying the corrected ray to estimator state
remains blocked on calibrated timing/extrinsics, replay, a reviewed
provenance/uncertainty treatment, and separate estimator/runtime/authority
reviews. Applying the Wave 3E measurement additionally remains blocked on the
full-homography producer/calibration, association and rate/state design, and
shared/sequential nuisance treatment.

Exercise a disposable clean candidate through T0-T4 scheduler leases,
interruption/resume, deduplication, exact worktrees, and merger evidence once
the operational prerequisites are available.

The first live change remains a bounded gate-1 recentering stage from the
captured high-right target. Do not jump directly to a full pass. It may use the
top-clipped aperture inference and pixel-motion damping only after recorded
replay and tracker-isolation acceptance, and must be a short authority-limited
maneuver that moves gate 1 toward a conservative image corridor, then stops and
proves cleanup. It must retain the same race/IMU/camera/collision watchdogs and
treat any gate-index change as an abort until a separate gate-1 crossing policy
is reviewed.

The current symmetric pixel-rate damping remains unchanged so the successful
gate-0 trajectory is reproducible. The observation shows that gate 1 is
available to the detector, while tracker state, not HSV segmentation, caused
the pre-credit rejection. The opt-in offline fitter now exposes the inner
aperture and treats clipped bbox support as censored, but it is not
runtime-wired, replay-accepted, or live-validated.

## Verification

Run the scoped suite before another live change:

```powershell
.\.venv\Scripts\python.exe -m pytest -q competition/tests estimation/tests gate_detection/tests planning/tests/test_vq2_guidance.py tests/test_aigp_vq2_runner.py tests/test_vision_udp.py tests/test_vision_udp_listener.py
```

The canonical exact candidate suite is:

```powershell
.\scripts\dev.cmd test-vq2
```

The maintenance integration at `ef92041` leaves the integrated Wave 3E
canonical scoped `test-vq2` policy byte-identical. It passes exactly `1,325`
tests in canonical candidate, pristine hash-pinned isolated, and post-merge
main runs. The Wave 3D count of `1,101`, Wave 3C count of `1,019`, Wave 3B
count of `910`, Wave 3 IMU count of `872`, Wave 2 count of `743`, Wave 1 count
of `418`, and earlier `294`-test count are retained only as historical
evidence.
