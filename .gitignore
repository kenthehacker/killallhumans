/external_data

# Python virtual envs and bytecode caches
.venv/
__pycache__/
*.py[cod]

# Simulation/demo generated binary artifacts
simulation/example_output/*.ppm
simulation/example_output/*.png
simulation/example_output/*.jpg
simulation/example_output/*.jpeg


gate_detection/training/dataset/images
gate_detection/training/dataset/
gate_detection/training/val_failures

# Telemetry logs
logs/

# Iter-009l (Opus Mi2): racing-line cache is a deterministic build
# artifact, regenerated on first cold run from gate geometry +
# select_velocity_mps. Committing it is misleading (last-writer-wins
# semantics; cache often ends up with a toy-test layout instead of
# race_01's offsets).
planning/racing_line_cache.json

# Iter-022: residual_weights.npz is a regenerable artifact (zero-init
# placeholder; future iters fit it from collected traces). The
# script `scripts/init_residual_weights.py` writes it on demand.
control/residual_weights.npz